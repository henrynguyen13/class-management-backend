export enum Common {
  PAGE = 1,
  PERPAGE = 10,
  SORT_BY_DESC = 'desc',
  SORT_BY_AESC = 'aesc',
  SORT_BY_CREATED_AT = 'createdAt',
}

export enum OrderDirection {
  ASC = 'ASC',
  DESC = 'DESC',
}

export enum Role {
  TEACHER = 'teacher',
  STUDENT = 'student',
}
